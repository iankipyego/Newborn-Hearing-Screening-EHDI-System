# Build Log — Mama Rachel EHDI System

Update this after every session, before you close the tab. This file — not
your chat history with any AI — is what lets you resume work with a
different model, a different account, or after a token limit resets.

When starting a new AI session, paste in:
1. This file (so it knows where things stand)
2. The relevant `context-packs/phase-X.md` file for what you're building next
3. The specific file(s) you want it to write — not "build phase 1A", but
   "write lib/pathway/engine.ts based on the attached spec"

---

## Current phase
Phase 1A — Data Foundation (not started)

## Done
- [ ] (nothing yet — fill this in as you go)

## In progress
- (what you're working on right now)

## Next up
- (the next single-file task, per the phase's context pack)

## Known issues / things to revisit
- (anything you noticed but didn't fix yet)

## Decisions locked in during this build (don't let a new AI session re-litigate these)
- Screen 1 → Screen 2 window: 14 days (confirmed)
- Parent survey retry policy: 1 attempt + 1 follow-up, then hard stop
- Stack: Next.js 14 (App Router) + TypeScript + PostgreSQL + Prisma
- NICU >5 days → AABR is hardcoded, not a clerk override

## Session history
| Date | What got built | Model/account used | Notes |
|---|---|---|---|
| | | | |
